name: unitdb_client
description: Lightweight and high performance publish-subscribe messaging system - dart client library.
version: 0.1.0+1

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  async: ^2.4.2
  grpc: ^3.0.2
  protobuf: ^2.0.0
  meta: ^1.1.8
  drift: ^1.3.0
  mutex: ^3.0.0
  # path_provider: ^2.0.7

# dependency_overrides:
#   grpc:
#     git:
#       url: https://github.com/a14n/grpc-dart.git
#       ref:  issue-147

dev_dependencies:
  drift_dev: ^1.0.2
  build_runner: ^2.1.4
